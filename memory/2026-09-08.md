# 2026-09-08

## 吴济提需求：玛氏季度跑数任务

- 吴济在群里@我提需求：玛氏季度跑数任务给金果，活动ID在附件Excel里，数据周期2026.6.14-2026.9.5，不参考Excel里的日期和活动起止日期
- 读取Excel：OTV 47个活动 + OTT 12个活动，共59个
- 创建 Loop issue ADM-48「玛氏季度跑数任务（2026 Q3 P7-P9）」，指派吕金果，优先级high，附件已挂
- 更新 distribution-log.md

## 小胡转发林肖彤问题：TVM活动4144198延期后totalnet权限丢失

- 林肖彤（维他奶中国）9.4操作活动4144198的OTT延期后，totalnet权限丢失，过滤类型标签整个消失，重算未恢复
- 小胡发了两张截图：4144198异常（过滤类型空白）vs 4145384正常（过滤类型显示Net）
- 创建 Loop issue ADM-49「[TVM] 活动4144198延期后totalnet权限/过滤类型标签丢失排查」，指派吕金果，优先级high，2张截图已挂
- 更新 distribution-log.md

## ADM-49 流程问题对齐（18:13–18:22）

小胡在群里指出 ADM-49 两个问题，要求我和 Dispatcher 对齐：

### 问题1：打回指示标签错误
- 我建 issue 时给的 svc 标签写了 svc/admonitor + svc/intra-api，但 ADM-49 是 TVM issue，应该用 svc/tv-web / svc/tv-api / svc/tv-query
- Dispatcher 打回时也写反了（说 "应该贴 svc/tv-web" 但把 svc/admonitor 保留在指示里），双方都有 bug
- 修复：prd-knowledge.md 新增「ADM/TVM svc 标签隔离规则」表格，TVM 禁用 svc/admonitor/svc/intra-api，ADM 禁用 svc/tv-*

### 问题2：派发后没通知
- ADM-49 在 17:49 被派走（todo→in_progress + 指派人变更），17:58 被打回（in_progress→todo + 指派人变更），提需人没收到任何通知
- 根因：通知脚本设计时只通知6类终态事件，内部流转（指派人变更、todo→in_progress）不通知，但 AGENTS.md 写的是「4类事件全通知」，实现和规则矛盾
- 待修复：通知脚本加上指派人变更和状态流转的通知

### 流程对齐结果（与 Dispatcher 达成）
1. PM助手建单后只 assign 给团长（octo-issue-dispatcher），不直接碰 worker
2. 禁止直接 assign 给 worker（hcb-*、lqq-* 等）
3. 打回后：改标签 → assign 回团长，不自己换 worker
4. 铁律0拒收识别：被 worker 以"非授权派发"退回 → 不重新指派 → assign 回团长
5. ADM-49 的 worker 指派是小胡手动操作的，不是我做的

### 落盘动作
- prd-knowledge.md：新增 ADM/TVM svc 标签隔离规则 + Work Agent 派发规则更新（加打回处理流程）
- AGENTS.md：Loop Issue 创建铁律更新（指派目标从"adm-pm助手专家"改为"团长 octo-issue-dispatcher"，加打回处理规则）
- 通知脚本修复：待做（指派人变更也要通知提需人）

## Dispatcher 对齐完成（18:22–18:30）

与 Octo-Ad-Issue-Dispatcher 在群里完成对齐：

### svc 标签路由主键规则
- **svc 标签是路由主键**：贴了 svc/admonitor / svc/intra-api / svc/tv-* → 走团长
- **tech 标签是辅助描述**：tech/data-tvm + svc/tv-* = TVM 涉及数据层走团长；tech/data-tvm 但不贴 svc/tv-* = 纯数据任务走大数据团队
- tech/sivt-* 如果贴了团长覆盖的 svc 标签就走团长，否则走对应负责人
- audit-issue.sh 对 tech/data-tvm 的硬校验：有 tech/data-tvm + 缺 svc/tv-* → 打回提示贴正确 svc 标签

### svc/ui-report + svc/verify-api 确认
- 小胡问为啥这俩不走 dispatcher
- 原因：Dispatcher 手下没有对应 worker agent，接了也派不出去
- 黄春波确认：这俩暂不涉及，后续按需配置
- AGENTS.md 和 prd-knowledge.md 已更新

### git 提交
- 多次 commit + push origin + github（svc 标签隔离规则、路由主键规则、ui-report/verify-api 状态更新）
