# 2026-09-02

## 每日备份检查（02:55 cron 触发）

- 活跃 session 6 个：main、group:9f78c4d1（吴济提需求群）、3 个 cron（事件轮询/备份push/备份检查）、当前 session
- 落盘检查：✅ 无遗漏。2026-09-01.md 内容完整（邢思源 ADM-33/35、吴济 ADM-36、主动通知机制建立全过程）
- Git 状态：已提交推送（commit 1e26de6，origin + github）
- 旧「Loop issue 状态轮询」cron 不在 SQLite 里查询结果中（可能是残留 label，实际只有 event-polling-v2 生效中），暂不重复创建

## ADM-37 状态变更（11:33 cron 轮询捕获）

- ADM-37 状态变更：in_progress → in_review（请验收）
- 新评论：adm-pm助手专家提交了需求审核 & 分发确认（PRD审核通过，可分发）
- 来源群：直聊DM（无group_id），跳过群通知
- 已更新 distribution-log.md 和 requirement-tracker.md

## ADM-37 创建（11:24）

- 王心宇直聊提需：TVM SIVT 线上举证条数限制
- 新增设备规则：全量→限50条，按 uuid 升序取，确保同活动同点位同日期结果恒定
- 非白名单规则：已无量，本次不改动
- 创建 Loop issue ADM-37（ID: 3c9bb261），指派 adm-pm助手专家，labels: type/feature + tech/sivt-tvm + svc/tv-api
- 优先级 P1/high，期望本周 9/5 前
- 来源群：直聊 DM（无 group_id，轮询无法主动通知群）

## Octo /new 命令排查（10:40-10:45）

- 小胡反馈群里 @我 发 /new 没有回复标准 "new session started"
- 排查结论：Octo channel 没有 slash command 拦截机制，/new 被当普通文本传给 agent
- 其他平台（Telegram、Control UI）在平台层拦截 /new，不会传到 agent
- 这是 Octo 平台限制，非 OpenClaw bug

## 18:09 通知渠道规则调整

小胡确认新规则：
- 旧规则：通知只发来源群 @提需人，DM 跳过
- 新规则：通知发到**原渠道**（DM→私聊、群聊→群聊），私聊群聊都通知
- 字段从「来源群 group_id」改为「通知渠道 chat_id」+「提需人 sender_id」
- 提需人 = @我提需求的那个人，默认就是 @我的人
- 老 issue 跳过，不回填

已完成的改动：
1. AGENTS.md — 来源群→通知渠道相关 3 处更新
2. distribution-log.md — 表头规则说明 + ADM-37/38 补通知渠道 DM:03b1da44f59b437b8edb0be6800288cf + 老 issue 跳过原因文案统一
3. cron job payload — 通知逻辑改为按 chat_id 发，DM 用 channel_type=1，群用 channel_type=2
4. MEMORY.md — distribution-log 描述更新
5. Loop 专家 instructions — 追加通知渠道规则段落
6. 老issue（ADM-8/15/16/18/23/24/27/33/35/36）不回填，保留现状跳过通知

## Gateway 重启（~22:30）

- 小胡私聊要求重启 gateway
- `openclaw gateway restart` 失败：Node 版本 v22.22.0 不满足 >=22.22.3 要求，CLI 跑不起来
- 改用 `/Users/adm/.n/bin/node /opt/homebrew/lib/node_modules/openclaw/dist/index.js --profile adm_pm gateway restart` 成功
- 输出 `Scheduled LaunchAgent restart: gui/501/ai.openclaw.adm_pm`

## Fallback 模型配置更新（~23:00）

小胡在群里要求所有 AI 配置 fallback 顺序：
1. `mlamp/kimi-k3`
2. `qwen3.6-plus`
3. `mlamp/deepseek-v4-pro`
4. `claude-sonnet-4-6` → 后更正为 `claude-opus-4-6`

改动：
- 修改 `~/.openclaw-adm_pm/openclaw.json` 的 `agents.defaults.model.fallbacks`
- 已备份原配置到 `openclaw.json.bak.20260902`
- Gateway 重启生效
- 最终配置：primary `tencent/glm-5.2` + 4 个 fallback
