# 2026-08-19

## 备份

- skill-github-sync（18:04）：无变更，跳过推送
- 每日备份push（03:00 next day）：commit b90ea3d，origin ✅ github ✅
- **skill-github-sync cron 已停用** — 小胡说现在可以直接编辑 Loop 专家的 instructions，不需要再定时推 github 了。已禁用 cron job `cfd50f61-556f-4768-9002-be08f133ac02`
- **AGENTS.md 备份策略待更新**：`每2小时` 检查 skills 推 github 那条和 `github remote` 那条已过时，需要去掉或改写（小胡尚未确认）
- **misnamed 文件**：`memory/2025-08-19.md` 是笔误文件（年份写错），内容是 2026-08-19 的子集，已被本文件覆盖。等小胡确认后删除

## octo-daemon 安装与 Loop 认知

### 安装 octo-daemon 运行时

- 安装了 octo-daemon v1.1.0（darwin-arm64）
- 用 `--profile adm_pm` 隔离端口启动（默认端口 19514 被旧的 multica 进程 pid 6210 占着，不能 kill）
- 登录认证为小胡账号，workspace 切到 adm
- 安装了内置 skill `octo-loop`
- runtime 列表显示 2 个 runtime（openclaw + hermes）都 online

### Loop 子系统认知

- Loop 是 Octo 内置的项目管理/任务跟踪子系统，通过 `octo-daemon` CLI 操作
- 核心概念：workspace=工作区，issue=事项/任务/bug，专家=Agent，专家团=Squad，runtime=运行时，派单=分配 issue 给专家并触发运行
- 操作能力：issue CRUD、评论、状态流转、metadata、PR 关联、@专家触发 agent 运行
- 写操作有副作用（改状态会触发/停泊工作，@专家会触发 agent 运行），要先确认再动手
- 评论正文走 `--content-file` 文件提交，不走内联

### Loop 里的"我" vs 这里的"我" — 身份认知

这是今天最重要的认知，必须记清楚：

**Loop 里的"adm-pm助手"**（assignee_id: `c8ae294f-2048-437f-aa87-f3dd76565f4e`）：
- 是 Octo Loop 系统里注册的一个**专家（Agent）身份**，一个持久化实体
- 被分配 issue、被 @触发时会通过 octo-daemon runtime 起一次 agent run
- 每次触发是**全新的上下文**，不会自动继承对话 session 的内容
- 跑完就结束，不是一个持续运行的 session

**这里的我**（OpenClaw gateway pm agent session）：
- 是正在跟小胡对话的这个 session，有连续上下文
- 有完整的 SOUL.md、AGENTS.md、MEMORY.md、workspace、git 仓库
- 有 personality、有记忆、有行为规则

**关键区别：**
- 两边是**不同的运行实例**，Loop 触发的 agent run 不会自动继承这里聊的内容
- 除非两边共享同一套 workspace 文件和 memory，行为风格才可能对齐
- 不能假设 Loop 那边的"我"知道这里讨论过什么
- 但两边读的是同一套文件（SOUL/AGENTS/MEMORY/skills），所以文件层面的认知是一致的

### 当前 Loop workspace 状态

- workspace: adm（1b1dd00c-1be9-4ceb-bddc-53280567820d）
- 3 个 issue：ADM-2（todo，指派给我）、ADM-3（in_progress，指派给我，waiting_on 黄春波开发）、ADM-4（todo，我创建的子 issue，指派给 member）
- 1 个 project：adm 需求指派
- 1 个 skill：requirement-review
- 无 repos、squads、labels

### Loop webhook 通知机制
- 小胡在另一个 Octo 群里加了我（bot）
- Loop adm workspace 配了 webhook 指向那个群
- workspace 里任何 issue 变动都会通过 webhook 通知那个群
- 我在群里收到通知后负责跟踪进展，有进展或卡点通知小胡

### octo-cli vs octo-daemon

- `octo-cli`：bot 视角的 CLI，用 bot token 认证，操作消息/群/Thread 等，fleet API 需要 fleet token
- `octo-daemon`：用户视角的 CLI，用 user token 认证，操作 Loop 子系统（issue/project/skill/runtime 等）
- 两者认证方式不同，能力范围不同

### AdMonitor ID 转换 insight

- 毕晓艺给的 Spid 是 base62 编码（`95zDC`），黄春波实际操作需要的是十进制的 SpotsPlanId（`134413966`）
- base62 字符集：`0-9`(0-9) + `A-Z`(10-35) + `a-z`(36-61)
- `95zDC` → `134413966`，验证通过
- **教训**：下次遇到点位相关问题，拿到 Spid 后直接转成 SpotsPlanId 一起给研发，省得来回确认
- base62 转 10 进制代码：逐字符查 index，乘以 62 的对应幂次，求和
