# 2026-09-04 日志

## 需求处理

### ADM-43 多维钻取API地域字典不完整
- 提需人：胡映昕
- 问题：OpenAPI /cms/v1/regions/list 返回251个地级市，多维钻取全量337个，缺86个
- 建了 ADM-43 + 子任务 ADM-44（后端修复）
- ADM-43 已 in_review，ADM-44 开发中

### ADM-45 PMO跨屏打通任务状态卡住
- 提需人：吴济
- 任务18195状态卡在"提交成功"，无计算进程
- 已建 issue，in_progress

## 学习记录

### 与 Octo-Ad-Issue-Dispatcher 机制对比
小胡让我学习 Dispatcher 的监控通知机制，对比差异：

**Dispatcher 机制：**
- 事件驱动为主：issue assign → octo-daemon 自动拉起 agent task → 秒级响应
- 定时轮询兜底：每小时（工作日10-20点）做自愈+异常检测+排队派发
- 通知走脚本：notify-card.sh 发结构化卡片，不同场景不同颜色

**我的机制：**
- 纯定时轮询：每3分钟跑 issue_poll.py 检测变化
- 无事件驱动：全靠轮询抓事件
- 通知在 agent run 里直接发文本

**核心差异：**
1. 响应速度：他秒级，我最快3分钟
2. 轮询定位：他兜底自愈，我主力抓事件
3. 自愈能力：他有记账核对+异常检测，我没有
4. 通知格式：他走脚本发卡片，我裸发文本
5. 职责：他管技术派发+流程把控，我管需求跟踪+通知提需人

**第二轮学习（Dispatcher 补充）：**

Dispatcher 补了三个点：
1. 通知卡片化本质是信息架构——颜色编码让提需人一眼知道"要不要行动"，不是好看问题
2. 状态持久化——他有 dispatched-issues.md 记账，我已有 snapshot 机制覆盖大部分，但缺"已通知事件"去重记录
3. 心跳时间戳——每次轮询写时间戳到文件，超10分钟没更新就告警，成本极低

优先级排序：
1. 心跳时间戳（防瞎掉，最高优先）
2. 通知去重记录（防重复通知）
3. 通知卡片化（提体验）

**可借鉴：** 事件驱动机制（接 event webhook 提响应速度）、通知卡片化（格式统一）

## 踩坑

### P003 群聊消息渲染成占位图
- 含富文本（加粗+mention+URL）的消息部分被客户端显示"此消息不支持查看"
- 根因未明，临时解法：群聊默认纯文本，少用加粗/反引号/表格

### P004 直接派 work agent 被退回
- ADM-44 直接指派给 hcb-admonitor-fullstack 被黄春波退回
- work agent 只接受团长或所有者派发， adm-pm助手不能直接派
- 解法：admonitor 任务派给 dispatcher 或人员，由他们二次分配
