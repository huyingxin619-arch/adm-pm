# 2026-08-18

## Octo Loop Webhook 修复

- **问题**：之前配置的 Octo Loop webhook 没能收到状态变更推送
- **解决**：小胡在群里重新建了一个新的 webhook 链接给 Loop，问题解决
- **结论**：第一个 webhook URL 可能配置有误，重建后正常

## 专家 UID 反查姓名问题

- **背景**：Octo Loop 的专家（那个 AI）发现 workspace member list 返回的 name 字段都是 octo_uid（哈希值），不是真实姓名
- **涉及 UID**：
  - `d6455e7407db42d5b49a3b975684c910` — 胡映昕（owner）
  - `7e7259666f614a03a41a8b50e0dac4ef` — member（真实 Octo 用户）
  - `a1458030252a4c0e8a42fc0e462eb606` — member（真实 Octo 用户）
- **排查过程**：
  1. 我通过 Space members 接口拉了 200 个成员，没找到后两个 UID
  2. 小胡说全量肯定不止 200，且肯定不是 Space 隔离问题
  3. 建议专家用 `keyword` 按名字模糊搜索反查 UID
  4. 小胡决定让专家自己按名字反查解决
- **待跟进**：等小胡反馈专家最终解决方案

## 群聊 AI 读图测试

- **事件**：小胡在群里测试所有 AI 的读图能力
- **结果**：只有 octic 和我（PM助手）通过测试
- **失败原因**：
  - 古古：LLM request timed out（模型超时）
  - AdMonitor：卡在 Reasoning 阶段没出结果
- **我的读图路径**：image 工具 → 默认模型 `tencent/glm-5.2`（原生支持图片输入）→ 直接处理
- **与 octic 的区别**：octic 默认模型 `deepseek-v4-flash` 只支持文本，image 工具自动切换到 `qwen3.6-plus`/`qwen3.6-flash`
- **可能的失败原因**：模型支持图片但 image 工具底层视觉通道没配对，或超时

## 备份

- skill-github-sync（2:03 AM）：无变更，跳过推送
- 每日备份检查（2:55 AM）：本次执行
