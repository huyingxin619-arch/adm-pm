# 2026-08-17

## 事件记录

### requirement-review skill 在 loop 里加载问题解决
- **问题**：`requirement-review` skill 在 runtime 的 skill loop 里拉不到内容，报错访问 `github.com/huyingxin619-arch/adm-pm` 的 `main` 分支失败
- **根因**：仓库是 private，runtime 拉取时无法认证
- **解决**：小胡把仓库改成 public 后，skill loop 里正常加载到了该 skill
- **结论**：skill 仓库需要 public（或配好认证）才能被 runtime 的 skill loop 正常加载

### skill 同步机制落盘
- **认知缺失**：之前没有"skill 更新后要推 github"的规则和定时任务
- **补齐**：
  1. 更新每日备份任务（凌晨3:00），同时推 origin + github
  2. 新增 skill-github-sync 任务（每2小时），检查 skills/ 等文件有变更就推 github
  3. AGENTS.md 备份策略章节更新
  4. TOOLS.md 增加 git remotes 信息
- **铁律**：skill 有更新必须及时推 github，loop 引用的才是最新版本
